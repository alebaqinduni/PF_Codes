
// to terminate:                word[x]!='\0' or  x<word.length()
// to use name with space:      getline(cin,name)
//(last letter check) if(name[name.length()-1]==letter) this iss // name.length()
//for first letter check if(name[0] == letter)
// sizeof(arr) for size and sizeof(arr)/value for n
//